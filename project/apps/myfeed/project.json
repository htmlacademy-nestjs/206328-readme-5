{
  "name": "myfeed",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/myfeed/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/myfeed",
        "main": "apps/myfeed/src/main.ts",
        "tsConfig": "apps/myfeed/tsconfig.app.json",
        "assets": ["apps/myfeed/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/myfeed/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "myfeed:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "myfeed:build:development"
        },
        "production": {
          "buildTarget": "myfeed:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/myfeed/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/myfeed/jest.config.ts"
      }
    }
  },
  "tags": []
}
